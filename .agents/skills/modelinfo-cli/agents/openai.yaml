interface:
  display_name: "modelinfo CLI"
  short_description: "Run modelinfo commands to inspect and compare model metadata quickly."
  default_prompt: "Use the modelinfo CLI to answer this request."
policy:
  allow_implicit_invocation: true
